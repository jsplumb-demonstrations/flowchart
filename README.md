# flowchart

This is a demonstration of a simple Flowchart, using the 4.x branch of jsPlumb.  There are two versions of the demo in this repository - `ts` contains a Typescript version, using Rollup + Babel, and `js` contains a 'vanilla' Javascript version.
